<form method="post" action="{{ path('partenaire_type_prestation_delete', {'id' : id , 'id_tp': type_prestation.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ type_prestation.id) }}">
    <button class="btn">Delete</button>
</form>
