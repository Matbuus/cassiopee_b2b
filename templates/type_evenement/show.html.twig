{% extends 'base.html.twig' %}

{% block title %}TypeEvenement{% endblock %}

{% block body %}
    <h1>TypeEvenement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_evenement.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ type_evenement.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('type_evenement_index') }}">back to list</a>

    <a href="{{ path('type_evenement_edit', {'id': type_evenement.id}) }}">edit</a>

    {{ include('type_evenement/_delete_form.html.twig') }}
{% endblock %}
