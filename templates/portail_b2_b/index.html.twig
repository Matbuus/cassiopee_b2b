{% extends 'base.html.twig' %}

{% block title %}PortailB2B index{% endblock %}

{% block body %}
    <h1>PortailB2B index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for portail_b2_b in portail_b2_bs %}
            <tr>
                <td>{{ portail_b2_b.id }}</td>
                <td>{{ portail_b2_b.nom }}</td>
                <td>
                    <a href="{{ path('portail_b2_b_show', {'id': portail_b2_b.id}) }}">show</a>
                    <a href="{{ path('portail_b2_b_edit', {'id': portail_b2_b.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('portail_b2_b_new') }}">Create new</a>
{% endblock %}
